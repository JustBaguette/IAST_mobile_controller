<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/idRLContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <!--on below line we are adding an image view-->

    <!--on below line we are creating progress bar-->
    <ImageView
        android:id="@+id/idIVLogo"
        android:layout_width="306dp"
        android:layout_height="174dp"
        android:layout_centerInParent="true"
        android:layout_marginStart="25dp"
        android:layout_marginLeft="25dp"
        android:layout_marginTop="25dp"
        android:layout_marginEnd="25dp"
        android:layout_marginRight="25dp"
        android:layout_marginBottom="25dp"
        android:contentDescription="TODO"
        android:src="@drawable/logo_iast"
        tools:ignore="ContentDescription,HardcodedText,ImageContrastCheck" />

    <ProgressBar
        android:id="@+id/idPBLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/idIVLogo"
        android:layout_centerInParent="true"
        android:indeterminateTint="@color/white"
        />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="289dp"
        android:layout_height="41dp"
        android:layout_below="@+id/idPBLoading"
        android:layout_alignBottom="@+id/idPBLoading"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="60dp"
        android:layout_marginTop="104dp"
        android:layout_marginEnd="62dp"
        android:layout_marginBottom="-145dp"
        android:text="@string/developed_by_iris"
        android:textAlignment="center"
        android:textSize="20dp"
        tools:ignore="TextSizeCheck" />

</RelativeLayout>